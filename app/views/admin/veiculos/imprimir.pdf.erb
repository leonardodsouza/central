<!doctype html>
<html>
  <head>
    <meta charset='utf-8' />

    <style>
        body {
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol";
        font-size: 12px;
        color: #212529;
        text-align: left;
        background-color: #fff;
    }
    table{
        width: 100%;
        border-collapse: collapse;
    }
    th{
        font-size: 16px;
        height: 50px;
    }
    th, td {
        padding: 5px;
    }
    table, th, td {
        border: 1px solid black;
    }
    </style>
  </head>
  <body>
    <div id="content">
        <div style="text-align: left;">
            <h1>Relatório</h1>
        </div>
        <hr>
        <h3>Veículos em estoque <br> <%= Date.today.strftime('%d-%m-%Y') %></h3>
        <table>
            <thead>
                <th>Cod.</th>
                <th>Marca</th>
                <th>Modelo</th>
                <th>Preço</th>
            </thead>
            <tbody>
                <% @veiculos.each do |veiculo| %>
                    <tr>
                        <td><%= veiculo.id %></td>
                        <td><%= veiculo.marca %></td>
                        <td><%= veiculo.modelo %></td>
                        <td>R$ <%= veiculo.preco %></td>
                    </tr>
                <%end%>
            </tbody>
        </table>

    </div>
  </body>
</html>

